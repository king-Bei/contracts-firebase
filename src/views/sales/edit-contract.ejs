<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <div class="container mt-4">
        <h1>編輯合約</h1>
        <p class="text-muted">合約 ID: <%= contract.id %></p>

        <form action="/sales/contracts/<%= contract.id %>/edit" method="POST">
            <div class="card">
                <div class="card-body">
                    <div class="mb-3">
                        <label for="customer_name" class="form-label">客戶姓名</label>
                        <input type="text" class="form-control" id="customer_name" name="customer_name" value="<%= contract.customer_name %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="customer_email" class="form-label">客戶 Email</label>
                        <input type="email" class="form-control" id="customer_email" name="customer_email" value="<%= contract.customer_email %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="variables" class="form-label">合約變數 (JSON 格式)</label>
                        <textarea class="form-control" id="variables" name="variables" rows="8"><%= JSON.stringify(contract.variables, null, 2) %></textarea>
                        <div class="form-text">請輸入此合約的客製化內容，使用 JSON 格式。</div>
                    </div>
                </div>
                <div class="card-footer text-end">
                    <a href="/sales/contracts/<%= contract.id %>" class="btn btn-secondary">取消</a>
                    <button type="submit" class="btn btn-primary">儲存變更</button>
                </div>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>